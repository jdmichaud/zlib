# zlib

This is [zlib](https://github.com/madler/zlib) packaged for [Zig](https://ziglang.org/).

# zig version compatibility

zig version 0.14.0

# Installation

Use `zig fetch`:
```bash
zig fetch --save git+https://github.com/allyourcodebase/zlib#master
```
